import 'package:flutter/material.dart';

class CustomRadioButto extends StatelessWidget {
  const CustomRadioButto({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}